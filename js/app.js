/*
//De quoi potentiellement tricher avec le contenu de "the_content"

const imgSingles = document.querySelectorAll(".single img");
const figures = document.querySelectorAll(".single figure");

imgSingles.forEach(imgSingle => {
    imgSingle.classList.add("col-12", "col-md-6");
});

figures.forEach(figure => {
    figure.classList.add("row");
});*/